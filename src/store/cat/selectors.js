export const selectCats = (reduxState) => reduxState.cat.catlist;
export const selectCatDetails = (reduxState) => reduxState.cat.details;
export const selectCategories = (reduxState) => reduxState.cat.categorylist;
